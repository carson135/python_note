# simple object
# int, float, bool(True, False), None

# iterable object (迭代 对象): string, list, tuple, dict, set, range
# string, # variable = "xxx",
# list, tuple, dict, set, range, # variable = [x,x,x], (x,x,x), {key:value,key:value,key:value}, {2,3,5} (set())

# immutable: int, float, bool, string, tuple
# mutable: list, dict, set

# order

# iterable object
# n = v[index] # string, list, tuple # dict [key], #for set object, because no order for items, can't use index to access items.
# v[index] = 'xxx', change one item value only for list, tuple, #dict[key]
# v[::-1], #
# for each_item in iterable_object, # for key, value in dict_object.items(), . keys(), .values()
# if value in iterable_object

# build-in functions: type convert, 
# type convert: list(), str(1), int(), float(), bool(), tuple(), dict(), set()
# print(), input(), len(), max(), min(), sum(), 

# string method: replace, split, join, +, f"xxx{}xxx", format, upper, strip, find, count()...
# list type methods: append(value), insert(index, value), pop(index), remove(value), sort(), reverse() 
# dict type: get, 

# others:sorted(l) reversed('abc') range(10) len()

# control keyword:
# if: True/False, ==, <, >, <=, >=, !=, and, or , not
# for x in xxx
# while (bool expression(V != value) / True)
# for x in xxx
#   if(bool expression)
#       break